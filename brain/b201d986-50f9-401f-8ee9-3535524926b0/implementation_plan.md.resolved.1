# Implementation Plan - Sakakibara Tax Website (Revision)

## Goal
Revise the first view to use **Horizontal Text** and reference WordPress SWELL theme's "Main Visual" block architecture while maintaining the premium design.

## Design Specs (Revision)
-   **Orientation**: Horizontal (Yokogaki).
-   **Structure**: Mimic SWELL's Main Visual.
    -   Centered or Left-aligned content container.
    -   Large Hero Image with overlay (Darkening).
-   **Typography**:
    -   Headings: `Noto Serif JP` (Keep for trustworthiness).
    -   Body: `Noto Sans JP`.
    -   Size: Large, impactful headings.
-   **Colors**: Keep existing (Deep Navy, Gold, Warm Gray).

## Proposed Changes

### [MODIFY] [index.html](file:///C:/Users/hangy/.gemini/antigravity/scratch/sakakibara-tax/index.html)
-   Remove `.u-vertical-text` classes.
-   Re-structure hero content to be horizontal-friendly (div stack).
-   Add a call-to-action button (common in SWELL MV).

### [MODIFY] [style.css](file:///C:/Users/hangy/.gemini/antigravity/scratch/sakakibara-tax/css/style.css)
-   Remove `writing-mode: vertical-rl`.
-   Update `.hero__content` flex direction to `column` or `row`.
-   Adjust `.hero__title` and `.hero__lead` spacing for horizontal flow.
-   Add specific "SWELL-like" button styles (rounded, gradients or solid colors).
-   Ensure responsiveness shifts to stacked view on mobile.

## Verification Plan
### Manual Verification
-   Render page.
-   Check horizontal rhythm and spacing.
-   Verify against "SWELL" aesthetic (clean block-based look).
-   Capture new screenshots.
