# 外部リンク処理の安全確認

公開ページから収集した全リンクの外部送信は、会議認証URL、従業員入口、配信設定などの混在により繰り返し停止した。停止した内容を別経路で送信しない。

認証付きURLの一部除外だけでは不十分。会社サイト内の問い合わせ候補だけに限定し、管理・会員・ログイン・配信関連を除外する。追加パラメータや共有先がある元URLは外部へ渡さず保留とする。安全確認の可否は実行結果で確認し、成功を推測しない。

大量のリンクを一度に読み込むと出力が切れてJSONが壊れる。重複を除き、小さい単位で読む。途中結果を保存して処理済み企業を再送しない。
